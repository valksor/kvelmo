name: chore
description: Template for maintenance tasks and chores
frontmatter:
  type: chore
agent: claude
git:
  branch_pattern: "chore/{key}--{slug}"
  commit_prefix: "[chore]"
workflow:
  skip_quality: true
