# This directory contains the bundled mehr binary (macOS/Linux only)
# CI builds populate this with platform-specific binaries before packaging.
# Windows builds don't include a binary - they use WSL.
